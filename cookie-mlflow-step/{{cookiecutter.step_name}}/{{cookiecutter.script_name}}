#!/usr/bin/env python
"""{{cookiecutter.long_description}}"""
import argparse


def go(args):
    raise NotImplementedError(
        'TODO: implement this optional step with local project input and output paths.'
    )


if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='{{cookiecutter.short_description}}')
{% for arg_name in cookiecutter.parameters.split(",") %}
    parser.add_argument('--{{arg_name}}', required=True, help='Value for {{arg_name}}')
{% endfor %}
    go(parser.parse_args())
